# API Documentation disabled for troubleshooting
knife4j:
  enable: false
  production: true
  basic:
    enable: false

# SpringDoc disabled for troubleshooting
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# Disable chunked transfer encoding, use fixed Content-Length
server:
  compression:
    enabled: false
  http2:
    enabled: false
  servlet:
    context-path: /xiaozhi-esp32-api
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  tomcat:
    max-http-form-post-size: 2MB
    max-swallow-size: 2MB
    use-fixed-length-response: true
    uri-encoding: UTF-8
  port: 8002

spring:
  http:
    encoding:
      force: true
      charset: UTF-8
      
  liquibase:
    enabled: true
    drop-first: false
    change-log: classpath:db/changelog/db.changelog-master.yaml
  datasource:
    druid:
      #MySQL
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/xiaozhi_esp32_server?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true
      username: root
      password: 123456
      initial-size: 10
      max-active: 100
      min-idle: 10
      max-wait: 6000
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: D7Xj810i1C
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: false
        wall:
          config:
            multi-statement-allow: true
jasypt:
  encryptor:
    password: P9Hx718z8L